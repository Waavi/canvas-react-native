import { t } from '@/lang'

const errors = {
	UNKNOWN: { title: t('error.unknown.title'), text: t('error.unknown.text') },
	UNAUTHORIZED: { title: t('error.unauthorized.title'), text: t('error.unauthorized.text') },
}

export default errors
